import { Mulish, Ovo, Palanquin_Dark } from "next/font/google";

export const palanquin_dark = Palanquin_Dark({
	weight: "400",
	subsets: ["latin"],
});

export const mulish = Mulish({
	weight: "400",
	subsets: ["latin"],
});

export const ovo = Ovo({
	weight: "400",
	subsets: ["latin"],
});